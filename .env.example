# Copie este arquivo para ".env" (uso local) ou configure a mesma variável nas
# configurações de "Environment Variables" do seu projeto na Vercel (para produção).
# NUNCA coloque a chave real dentro de um arquivo que vai pro navegador ou pro Git.

ANTHROPIC_API_KEY=sk-ant-api03-substitua-pela-sua-chave-aqui
